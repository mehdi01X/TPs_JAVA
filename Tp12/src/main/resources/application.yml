server:
  port: 8091
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tp12_springboot?createDatabaseIfNotExist=true
    username: root
    password:

  jpa:
    show_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create